AGENT_STELLAR_SECRET=your_agent_secret_key
STELLAR_NETWORK=testnet
STELLAR_RPC_URL=https://soroban-testnet.stellar.org
STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
LODESTAR_API_URL=http://localhost:3001
# HMAC secret for signing payment-record requests (must match backend's SERVER_STELLAR_SECRET)
LODESTAR_HMAC_SECRET=your_server_secret_key

# Optional agent identity (displayed in logs and registered on-chain)
AGENT_NAME=LodestarAgent
AGENT_DESC=Autonomous x402 agent powered by Lodestar service discovery

AGENT_MAX_PER_TX=0.001
AGENT_MAX_PER_DAY=1.00

AGENT_ALLOWED_CATEGORIES=weather,search

<<<<<<< HEAD
# Service selection hardening
# Minimum reputation a service must have to be eligible for payment (default: 0)
AGENT_MIN_SERVICE_REPUTATION=0
# Max candidates to consider (weighted random) and max retry attempts per task (default: 3)
AGENT_MAX_SERVICE_RETRIES=3
=======
# Require scoring/spending policy enforcement (default: true)
# When true, the agent will exit with error if registration fails, preventing
# unprotected agent runs without spending limits. Set to false only for
# development/testing where you explicitly want to run without policy enforcement.
AGENT_REQUIRE_SCORING=true


>>>>>>> c45745f (Security: Make scoring a hard requirement by default)
